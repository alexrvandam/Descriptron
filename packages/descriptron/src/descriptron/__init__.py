"""Descriptron: a convenience package that installs core, vision and gui together.

There is no code here on purpose. Import from the distributions themselves:

    from descriptron_core.cli import data_path, available
    from descriptron_vision.cli import require
"""
__version__ = "2.0.0"
